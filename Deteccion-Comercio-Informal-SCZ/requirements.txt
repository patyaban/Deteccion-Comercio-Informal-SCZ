# Entorno exacto usado para producir los resultados reportados en el informe.
# Entrenamiento: Google Colab, Python 3.12.13, GPU NVIDIA Tesla T4 (CUDA 12.8).
#
# Instalación:   pip install -r requirements.txt
#
# Nota sobre torch: la rueda de PyTorch depende de la versión de CUDA de la
# máquina. La línea de abajo instala la build genérica (CPU o CUDA según el
# índice por defecto de pip). Para reproducir el entorno de GPU exacto:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
# Para inferencia en CPU (notebooks/inferencia_cpu.ipynb) basta la build CPU:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cpu

ultralytics==8.4.123
torch==2.11.0
torchvision==0.26.0
numpy==2.0.2
pandas==2.2.2
matplotlib==3.10.0
opencv-python-headless==4.11.0.86
Pillow==11.1.0
PyYAML==6.0.2
tqdm==4.67.1

# Solo para abrir los notebooks
jupyter==1.1.1
ipykernel==6.29.5
